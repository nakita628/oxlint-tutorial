// Sort imports and sort keys samples.
import { pickLabel } from './ternary.ts'
import { compute } from './naming.ts'

export const settings = {
  zoom: 1,
  alpha: 2,
}

export const used = { compute, pickLabel }
